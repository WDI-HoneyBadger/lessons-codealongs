<%= form_with(model: cup, local: true) do |form| %>
  <% if cup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cup.errors.count, "error") %> prohibited this cup from being saved:</h2>

      <ul>
      <% cup.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="field">
    <%= form.label :capacity %>
    <%= form.text_field :capacity %>
  </div>

  <div class="field">
    <%= form.label :material %>
    <%= form.text_field :material %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
